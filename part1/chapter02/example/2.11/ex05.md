# OR AND OR 연산자로 구성된 표현식

아래 코드의 결과를 예측해 보세요.

```javascript
alert(null || 2 && 3 || 4);
```

답 : 3

**&& 연산자는 || 보다 우선 순위가 높다.**

이 사실을 알고 다음과 같이 따라가보자.
`2 && 3` 은 3으로 평가된다. (falsy가 없으면 가장 마지막의 truly 값)
그다음 `null || 3 || 4` 는 가장 처음의 truly 값으로 평가되므로, 3이 답이다.
